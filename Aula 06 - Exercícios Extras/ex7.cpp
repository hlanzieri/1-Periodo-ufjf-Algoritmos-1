#include <bits/stdc++.h>

using namespace std;

bool verificaExist(int valor, int vet[], int cont){
    for(int i = 0; i < cont; i++){
        if(valor == vet[i]) return true;
    }
    return false;
}

int main () {
    int tamanho = 0, cont = 0;
    while(cin >> tamanho && tamanho <= 100)
    int vet[tamanho] = {0};
    while(cont < tamanho){
        // Continuar
    }
    return 0;
}